// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a datasource to retrieve all User(s).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as nutanix from "@pierskarsenbarg/nutanix";
 *
 * const list_users = nutanix.getUsersV2({});
 * const filtered_users = nutanix.getUsersV2({
 *     filter: "username eq 'username-example'",
 * });
 * ```
 *
 * ## Argument Reference
 *
 * The following arguments are supported:
 *
 * * `page`: - A URL query parameter that specifies the page number of the result set. It must be a positive integer between 0 and the maximum number of pages that are available for that resource. Any number out of this range might lead to no results.
 * * `limit` : A URL query parameter that specifies the total number of records returned in the result set. Must be a positive integer between 1 and 100. Any number out of this range will lead to a validation error. If the limit is not provided, a default value of 50 records will be returned in the result set.
 * * `filter` :A URL query parameter that allows clients to filter a collection of resources. The expression specified with \$filter is evaluated for each resource in the collection, and only items where the expression evaluates to true are included in the response. Expression specified with the \$filter must conform to the OData V4.01 URL conventions. For example, filter '\$filter=name eq 'karbon-ntnx-1.0' would filter the result on cluster name 'karbon-ntnx1.0', filter '\$filter=startswith(name, 'C')' would filter on cluster name starting with 'C'. The filter can be applied to the following fields:
 *     * createdBy
 *     * displayName
 *     * emailId
 *     * extId
 *     * firstName
 *     * idpId
 *     * lastName
 *     * lastUpdatedBy
 *     * status
 *     * userType
 *     * username
 * * `orderby` : A URL query parameter that allows clients to specify the sort criteria for the returned list of objects. Resources can be sorted in ascending order using asc or descending order using desc. If asc or desc are not specified, the resources will be sorted in ascending order by default. For example, '\$orderby=templateName desc' would get all templates sorted by templateName in descending order. The orderby can be applied to the following fields:     * createdBy
 *     * createdTime
 *     * displayName
 *     * emailId
 *     * extId
 *     * firstName
 *     * lastLoginTime
 *     * lastName
 *     * lastUpdatedTime
 *     * userType
 *     * username
 * * `select` : A URL query parameter that allows clients to request a specific set of properties for each entity or complex type. Expression specified with the \$select must conform to the OData V4.01 URL conventions. If a \$select expression consists of a single select item that is an asterisk (i.e., *), then all properties on the matching resource will be returned. following fields:
 *     * additionalAttributes
 *     * bucketsAccessKeys
 *     * createdBy
 *     * createdTime
 *     * displayName
 *     * emailId
 *     * extId
 *     * firstName
 *     * idpId
 *     * isForceResetPasswordEnabled
 *     * lastLoginTime
 *     * lastName
 *     * lastUpdatedBy
 *     * lastUpdatedTime
 *     * links
 *     * locale
 *     * middleInitial
 *     * region
 *     * status
 *     * tenantId
 *     * userType
 *     * username
 */
export function getUsersV2(args?: GetUsersV2Args, opts?: pulumi.InvokeOptions): Promise<GetUsersV2Result> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("nutanix:index/getUsersV2:getUsersV2", {
        "filter": args.filter,
        "limit": args.limit,
        "orderBy": args.orderBy,
        "page": args.page,
        "select": args.select,
    }, opts);
}

/**
 * A collection of arguments for invoking getUsersV2.
 */
export interface GetUsersV2Args {
    filter?: string;
    limit?: number;
    orderBy?: string;
    page?: number;
    select?: string;
}

/**
 * A collection of values returned by getUsersV2.
 */
export interface GetUsersV2Result {
    readonly filter?: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly limit?: number;
    readonly orderBy?: string;
    readonly page?: number;
    readonly select?: string;
    /**
     * List all User(s).
     */
    readonly users: outputs.GetUsersV2User[];
}
/**
 * Provides a datasource to retrieve all User(s).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as nutanix from "@pierskarsenbarg/nutanix";
 *
 * const list_users = nutanix.getUsersV2({});
 * const filtered_users = nutanix.getUsersV2({
 *     filter: "username eq 'username-example'",
 * });
 * ```
 *
 * ## Argument Reference
 *
 * The following arguments are supported:
 *
 * * `page`: - A URL query parameter that specifies the page number of the result set. It must be a positive integer between 0 and the maximum number of pages that are available for that resource. Any number out of this range might lead to no results.
 * * `limit` : A URL query parameter that specifies the total number of records returned in the result set. Must be a positive integer between 1 and 100. Any number out of this range will lead to a validation error. If the limit is not provided, a default value of 50 records will be returned in the result set.
 * * `filter` :A URL query parameter that allows clients to filter a collection of resources. The expression specified with \$filter is evaluated for each resource in the collection, and only items where the expression evaluates to true are included in the response. Expression specified with the \$filter must conform to the OData V4.01 URL conventions. For example, filter '\$filter=name eq 'karbon-ntnx-1.0' would filter the result on cluster name 'karbon-ntnx1.0', filter '\$filter=startswith(name, 'C')' would filter on cluster name starting with 'C'. The filter can be applied to the following fields:
 *     * createdBy
 *     * displayName
 *     * emailId
 *     * extId
 *     * firstName
 *     * idpId
 *     * lastName
 *     * lastUpdatedBy
 *     * status
 *     * userType
 *     * username
 * * `orderby` : A URL query parameter that allows clients to specify the sort criteria for the returned list of objects. Resources can be sorted in ascending order using asc or descending order using desc. If asc or desc are not specified, the resources will be sorted in ascending order by default. For example, '\$orderby=templateName desc' would get all templates sorted by templateName in descending order. The orderby can be applied to the following fields:     * createdBy
 *     * createdTime
 *     * displayName
 *     * emailId
 *     * extId
 *     * firstName
 *     * lastLoginTime
 *     * lastName
 *     * lastUpdatedTime
 *     * userType
 *     * username
 * * `select` : A URL query parameter that allows clients to request a specific set of properties for each entity or complex type. Expression specified with the \$select must conform to the OData V4.01 URL conventions. If a \$select expression consists of a single select item that is an asterisk (i.e., *), then all properties on the matching resource will be returned. following fields:
 *     * additionalAttributes
 *     * bucketsAccessKeys
 *     * createdBy
 *     * createdTime
 *     * displayName
 *     * emailId
 *     * extId
 *     * firstName
 *     * idpId
 *     * isForceResetPasswordEnabled
 *     * lastLoginTime
 *     * lastName
 *     * lastUpdatedBy
 *     * lastUpdatedTime
 *     * links
 *     * locale
 *     * middleInitial
 *     * region
 *     * status
 *     * tenantId
 *     * userType
 *     * username
 */
export function getUsersV2Output(args?: GetUsersV2OutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetUsersV2Result> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("nutanix:index/getUsersV2:getUsersV2", {
        "filter": args.filter,
        "limit": args.limit,
        "orderBy": args.orderBy,
        "page": args.page,
        "select": args.select,
    }, opts);
}

/**
 * A collection of arguments for invoking getUsersV2.
 */
export interface GetUsersV2OutputArgs {
    filter?: pulumi.Input<string>;
    limit?: pulumi.Input<number>;
    orderBy?: pulumi.Input<string>;
    page?: pulumi.Input<number>;
    select?: pulumi.Input<string>;
}
